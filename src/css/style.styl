body
  // Baseline Material-UI font treatment. We define it here as well,
  // because we otherwise get FOUT when hard-refreshing a page with
  // markdown->html set via `dangerouslySetInnerHtml` (e.g., one of the
  // product or team pages.
  color rgba(54, 50, 48, 0.87)
  font-size 0.875rem
@font-face {
    font-family: "StayClassyDuoSerrif";
    src: url("../fonts/StayClassyDuoSerif.otf");
}
  font-weight 400
  line-height 1.5
  letter-spacing: 0.01071em
  #logo
    background-color #fff
    &:hover
      background-color #eee
    #logoIcon
      background-color #f00
      color #fff

  a
    text-decoration none

  button
    a
      color #fff

  footer
    text-align center
